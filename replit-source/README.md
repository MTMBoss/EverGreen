# Replit Source

Sorgente importata dal branch `evergreen-hub-test`.

Questa cartella serve come base di migrazione grafica e componentistica:

- `artifacts/evergreen-hub`
  UI/UX da cui prendere layout, pagine e componenti
- `artifacts/api-server`
  riferimento per eventuali API future
- `lib/*`
  librerie condivise importate dal workspace Replit

Il progetto reale che continua a girare su Northflank resta quello nella root del
repository (`app/`, `attendance/`, `matches/`, `web/`, ecc.).
