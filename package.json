{
  "name": "evergreen-discord-bot",
  "version": "1.1.0",
  "description": "Discord bot for match publication and weekly training schedule automation",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "deploy": "node scripts/deploy-commands.js",
    "lint": "node --check src/index.js && node --check scripts/deploy-commands.js && node --check tests/*.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "discord",
    "discord.js",
    "bot"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.25.1",
    "dotenv": "^17.3.1",
    "node-cron": "^4.2.1"
  }
}
